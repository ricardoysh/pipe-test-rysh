name: set-vars

on:
  workflow_call:

    outputs:
      sds:
        value: ${{ jobs.get-vars.outputs.sds }}

jobs:
  set-vars:
    runs-on: macos-latest
    outputs:
      sds: ${{ steps.get-vars.outputs.saudacao }}
      ###### 
    steps:
        ####### SET VARS ###### SEND ENV ######
      - name: set vars
        id: set-vars
        run: |
          echo "## Set Vars..."
          echo "saudacao=Olá tudo bem?" >> "$GITHUB_ENV"
        ######### {{ steps.set-vars.outputs.saudacao }}
  
      - name: set outputs
        id: resolved
        run: |
          echo "Saudacao: ${{ env.saudacao }}"
          echo "sds: ${{ env.saudacao }}" >> GITHUB_OUTPUT
