name: pipeline-de-testes

on:
  push


jobs:
  vars:
    uses: ./.github/workflows/set-vars.yml


      # sds: {{ steps.vars.outputs.sds }}

  job-1:
    needs: vars
    runs-on: macos-latest
    steps:
      - id: xpto
        run: |
          echo "## Set Vars "
          echo "${{ needs.vars.outputs.sds }}"
